{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "R.I.M.A Web Controller (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}/public",
      "preLaunchTask": "Jalankan Semua Server"
    }
  ]
}
